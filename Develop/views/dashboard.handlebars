{{#if logged_in}}
  <h2>Your Dashboard</h2>
  <button id="logout">Logout</button>
{{else}}
  <a href="/login">Login</a>
  <a href="/signup">Sign Up</a>
{{/if}}

<h1>Your Posts</h1>
{{#each userPosts}}
  <div class="post">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
  </div>
{{/each}}
